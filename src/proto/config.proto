syntax = "proto2";

import "action.proto";
import "plant.proto";
import "pump.proto";

package otto;

message Config {
  repeated Plant plants = 1;
  repeated Pump pumps = 2;
  repeated Action actions = 3;
}
